plugins {
    id("airship-publish")
}

description = "Airship Android SDK preference extension."

apply from: "../common-build.gradle"

dependencies {
    api project(':urbanairship-core')
    api "androidx.preference:preference:$rootProject.androidxPreferenceVersion"
}
