/**
 * Airship Layout Rendering Library.
 *
 * <p>The custom view API -- {@code AirshipCustomViewManager}, {@code AirshipCustomViewHandler} and
 * {@code AirshipCustomViewArguments} -- is public. Everything else in this package and its
 * subpackages is internal, and is marked {@code @RestrictTo(LIBRARY_GROUP)} per declaration or per
 * package.
 */
package com.urbanairship.android.layout;
