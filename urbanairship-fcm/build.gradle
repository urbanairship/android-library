description = "Urban Airship Android SDK FCM plugin"

apply from: "../common-build.gradle"

dependencies {
    api project(':urbanairship-core')
    implementation "com.google.firebase:firebase-messaging:${airshipProperties.firebaseFcmVersion}"
    implementation "com.google.android.gms:play-services-base:${airshipProperties.playServicesBaseVersion}"
}
